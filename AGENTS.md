# AGENTS.md

- Python package name: `private-repo-hunter` (see `pyproject.toml`).
- Requires Python `>=3.14` (see `pyproject.toml`).
- Entry point script is `rh/main.py` (only current runnable code path).
- Other modules (`rh/db.py`, `rh/apis/github.py`, `rh/datasets/joss.py`) are stubs or empty.
